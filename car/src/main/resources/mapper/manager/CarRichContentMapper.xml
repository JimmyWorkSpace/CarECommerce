<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cc.carce.sale.mapper.manager.CarRichContentMapper">

    <!-- 插入富文本内容 -->
    <insert id="insert" parameterType="cc.carce.sale.entity.CarRichContentEntity" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO car_rich_content (
            title, content, contentType, showOrder, delFlag, createTime
        ) VALUES (
            #{title}, #{content}, #{contentType}, #{showOrder}, #{delFlag}, #{createTime}
        )
    </insert>

    <!-- 根据ID查询富文本内容 -->
    <select id="selectById" resultType="cc.carce.sale.entity.CarRichContentEntity">
        SELECT 
            id, title, content, contentType, showOrder, delFlag, createTime
        FROM car_rich_content 
        WHERE id = #{id} AND delFlag = 0
    </select>

    <!-- 更新富文本内容 -->
    <update id="updateById" parameterType="cc.carce.sale.entity.CarRichContentEntity">
        UPDATE car_rich_content SET
            title = #{title},
            content = #{content},
            contentType = #{contentType},
            showOrder = #{showOrder}
        WHERE id = #{id} AND delFlag = 0
    </update>

    <!-- 根据ID删除富文本内容（逻辑删除） -->
    <update id="deleteById">
        UPDATE car_rich_content SET delFlag = 1 WHERE id = #{id}
    </update>

    <!-- 根据内容类型查询富文本内容列表 -->
    <select id="selectByContentType" resultType="cc.carce.sale.entity.CarRichContentEntity">
        SELECT 
            id, title, content, contentType, showOrder, delFlag, createTime
        FROM car_rich_content 
        WHERE contentType = #{contentType} AND delFlag = 0
        ORDER BY showOrder ASC, createTime DESC
    </select>

    <!-- 查询所有富文本内容列表（未删除） -->
    <select id="selectAll" resultType="cc.carce.sale.entity.CarRichContentEntity">
        SELECT 
            id, title, content, contentType, showOrder, delFlag, createTime
        FROM car_rich_content 
        WHERE delFlag = 0
        ORDER BY contentType ASC, showOrder ASC, createTime DESC
    </select>

    <!-- 根据内容类型和排序查询富文本内容列表 -->
    <select id="selectByContentTypeOrderByShowOrder" resultType="cc.carce.sale.entity.CarRichContentEntity">
        SELECT 
            id, title, content, contentType, showOrder, delFlag, createTime
        FROM car_rich_content 
        WHERE contentType = #{contentType} AND delFlag = 0
        ORDER BY showOrder ASC, createTime DESC
    </select>

</mapper>
